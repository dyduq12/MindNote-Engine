/*
 * engine.geometry.js
 * Responsabilidade (Fase 3 - ainda nao implementada):
 * Passagem bottom-up (H_ramo = max(H_propria, soma(H_ramo(filhos))))
 * e passagem top-down para fixar coordenadas X/Y definitivas,
 * alem do calculo dos pontos de controle das curvas de Bezier.
 * Este arquivo hoje contem apenas stubs.
 */

function calcularReservaVertical(no) {
  throw new Error('calcularReservaVertical ainda nao implementado (Fase 3).');
}

function calcularCoordenadas(noRaiz) {
  throw new Error('calcularCoordenadas ainda nao implementado (Fase 3).');
}
